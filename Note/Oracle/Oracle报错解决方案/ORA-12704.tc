ORA-12704: character set mismatch
ORA-12704: 字符集不匹配
解决方法: to_char()
例子: CASE WHEN INSTR(TEM.IPAREA,'-',1,1)=0 THEN '-' ELSE TO_CHAR(SUBSTR(TEM.IPAREA,INSTR(TEM.IPAREA,'-',1,1)+1,LENGTH(TEM.IPAREA))) END AS CITY,